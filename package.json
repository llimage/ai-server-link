{
  "name": "ai-server",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "prisma:generate": "pnpm exec prisma generate --schema ./infrastructure/prisma/schema.prisma",
    "prisma:migrate": "pnpm exec prisma migrate dev --schema ./infrastructure/prisma/schema.prisma",
    "db:seed": "tsx ./infrastructure/prisma/seed.ts"
  },
  "devDependencies": {
    "@prisma/client": "^6.7.0",
    "@types/node": "^22.10.2",
    "prisma": "^6.7.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
