未完成项：
- prisma:migrate 未成功执行（spawn EPERM）
- main-server / workspace tests 未通过（spawn EPERM）

阻塞项：
- Prisma schema-engine 启动与 node:test 执行触发 spawn EPERM

风险点：
- retrieval_logs 新增字段未落库，检索日志无法完整记录
- hybrid/vector 检索与 LanceDB 写入未得到实测验证

补充：
- pnpm install 失败（EPERM unlink 临时文件）
- prisma:migrate 失败（schema-engine spawn EPERM）

补充：
- corepack 缺失（MODULE_NOT_FOUND），导致无法执行 build/test

补充（Round-04.2）：
- corepack 缺失导致 pnpm root -w/build/test 无法执行
- workspace root 校验无法完成

补充（direct pnpm 阶段）：
- pnpm 不在 PATH，无法执行 pnpm root/build/test
- npm 安装缺失，无法使用 npm 安装 pnpm
补充（Round-04.5）：
- pnpm 仍不可用，无法执行 build/test 链路
- workspace root 校验与测试执行持续阻塞
补充（Round-04.13）：
- pnpm 不在 PATH，导致 prisma/test 无法执行
- 需要提供可用 pnpm 路径或在当前 shell 中可执行

[Round-04.5] 若当前 shell 无 pnpm PATH，测试命令将失败并被原样记录在 05_commands_output.txt，需要在用户本机环境中再次执行。

[Round-04.5b] 当前 shell 未识别 pnpm，导致无法在本机完成 main-server 与全局测试；需在用户可用环境中执行并回填输出。

[Round-04.5d] 若本地未安装依赖导致 node_modules/.bin/tsx 不存在，测试仍会失败；需先 pnpm install。

[Round-04.5e] 若测试仍失败，需继续核查 ingestion index 返回 failed 的具体 errorMessage（可从 ingestion_logs/ingestion_tasks 查询）。
[Round-04.14] updateSourceStatus 如改用 updateMany 可能掩盖 not found；需结合日志验证再决定是否保留候选方案。
[Round-04.15] LanceDB 旧表仍含 metadata.tenant_id schema，需清理本地 LanceDB 目录以避免旧 schema 继续触发 append 失败。
